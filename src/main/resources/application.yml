spring:
  application.name: sisepe
  datasource:
    url: jdbc:postgresql://localhost:5432/eleitorium
    username: postgres
    password: Ka.54123
  jpa.hibernate.ddl-auto: validate
  flyway:
    baseline-on-migrate: true
  main:
    web-application-type: servlet

server:
  port: 8082
  servlet:
    context-path: /
  error:
    include-stacktrace: never
